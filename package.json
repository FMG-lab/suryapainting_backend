{
  "name": "surya-painting-api",
  "version": "1.0.0",
  "type": "module",
  "description": "Backend API untuk Surya Painting App",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "vercel-build": "echo 'Serverless functions ready'",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed-branches.js",
    "check": "node scripts/check-branches.js",
    "test": "echo 'Run tests here'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {}
}
